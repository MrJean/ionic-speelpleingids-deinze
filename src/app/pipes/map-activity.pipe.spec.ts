import { MapActivityPipe } from './map-activity.pipe';

describe('ActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new MapActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
